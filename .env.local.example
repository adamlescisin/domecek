# Database (Hostinger MySQL)
DB_HOST=hostname
DB_USER=username
DB_PASSWORD=password
DB_NAME=database_name

# Admin Auth
ADMIN_PASSWORD_HASH="$2b$10$..."     # bcrypt hash — generate with: node -e "const bcrypt = require('bcrypt'); bcrypt.hash('your-password', 10).then(console.log)"
JWT_SECRET="your-256-bit-random-secret"

# Stripe
STRIPE_SECRET_KEY="sk_live_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Email (Resend)
RESEND_API_KEY="re_..."
EMAIL_FROM="obchod@domecekujosefa.cz"
ADMIN_EMAIL="domecekujosefa@gmail.com"

# App
NEXT_PUBLIC_APP_URL="https://shop.domecekujosefa.cz"
